# 감사·검토 기록

날짜가 붙은 **스냅샷 문서**들이다. 각 문서는 그 시점의 코드를 기준으로 쓰였고,
이후 코드가 바뀌어도 고쳐 쓰지 않는다 — 판단의 근거와 시점을 함께 남기는 게 목적이라
현재 상태를 알려면 코드를 봐야 한다.

| 문서 | 시점 | 무엇을 다루나 | 지금 상태 |
|---|---|---|---|
| [`2026-08-16-codebase-audit.md`](2026-08-16-codebase-audit.md) | 리디자인 **이전** | 아키텍처·권한·감사로그·테스트 전면 감사 (C/M/D 항목) | 대부분 처리됨. 일부러 남긴 것은 문서 머리에 명시 |
| [`2026-08-25-codebase-audit.md`](2026-08-25-codebase-audit.md) | 상벌점 통계·최근 부여 이후 | 9개 관점 전수 감사 + 적대적 반증 (확정 41 / 기각 13) | **확정분 전부 수정됨.** 마이그레이션 적용이 남았다 — 문서 §5 |
| [`2026-08-26-codebase-audit.md`](2026-08-26-codebase-audit.md) | 학번 표기·문구 정리 이후 | 5개 관점 전수 감사 (확정 18) | **15건 수정됨.** 3건은 고치지 않기로 판단 — 문서 §4. 사람이 할 일은 §7 |
| [`2026-08-28-feature-sweep.md`](2026-08-28-feature-sweep.md) | 커뮤니티 이후 | **코드가 아니라 돌아가는 앱을 눌러 본 기록** — 라우트×역할 188 판정 + 조작 96 검사 (확정 2) | **2건 다 수정됨** — 문서 §3. 검사가 개발 DB를 건드린 사고와 복구는 §7 |
| [`2026-08-31-functional-uiux-sweep.md`](2026-08-31-functional-uiux-sweep.md) | 테스트 서버 배포·UI 원복 이후 | 학생·학부모·교사·관리자 병렬 실조작 + 교차 역할 출입증 + 모바일 UI/UX 점검 | **수정·재검증 완료** — 확정 결함과 개선 항목 처리 결과는 문서 §9 |
| [`2026-08-31-codebase-audit.md`](2026-08-31-codebase-audit.md) | 커뮤니티 이후 · UI/UX 수정 반영 후 | 여덟 영역 병렬 코드 감사 + 재검증 (확정 53 — 높음 2 / 중간 26 / 낮음 25, 기각·격하 3). **커뮤니티를 다루는 첫 감사 문서** | **높음·중간 처리됨** ([`2026-09-01-fix-batch.md`](2026-09-01-fix-batch.md)). 남은 것과 사람이 정할 것은 그 문서 §4·§6 |
| [`2026-08-31-codebase-audit-deep.md`](2026-08-31-codebase-audit-deep.md) | 위 감사 직후 (같은 기준선) | 같은 코드를 **모듈 축 + 관심사 축**으로 겹쳐 읽고 적대적으로 반증한 4라운드 감사 (확정 113 → 본문 112 — 높음 1 / 중간 17 / 낮음 94, 기각 2) | **높음·중간 처리됨** ([`2026-09-01-fix-batch.md`](2026-09-01-fix-batch.md)). 지난 감사와의 대조는 이 문서 §4 |
| [`2026-09-01-full-read-audit.md`](2026-09-01-full-read-audit.md) | 위 감사 직후 | **파일 순서 축** — 추적 파일 547개 78,598줄을 8등분해 한 줄도 건너뛰지 않고 통독 (확정 48 — 높음 0 / 중간 11 / 낮음 37, 그중 40건은 검색으로 못 찾는 것) | **중간 처리됨** ([`2026-09-01-fix-batch.md`](2026-09-01-fix-batch.md)). 방법 자체의 소득과 한계는 이 문서 §5 |
| [`2026-09-01-fix-batch.md`](2026-09-01-fix-batch.md) | 위 넷을 처리한 뒤 | **감사 문서가 아니라 처리 기록이다** — 확정분 중 높음·중간을 고친 결과, 검증에서 드러난 것, 남긴 것과 그 이유 | 완료 (`npm run verify` 통과) |
| [`2026-09-01-verification-pass.md`](2026-09-01-verification-pass.md) | 위 세 감사 직후 | 세 감사의 확정 항목 212건을 검증자 여섯이 재판정(확정 210 / 격하 1 / 기각 1) + 세 문서에 걸친 중복을 접어 실결함 수를 낸다 (28개 클러스터, 실제 177건) | 기각·격하 근거와 중복 클러스터는 문서 §3~§5. **확정률이 210/212라 신뢰도가 낮다** — 그 유보는 문서에 함께 적혀 있다 |
| [`2026-09-01-vertical-full-read.md`](2026-09-01-vertical-full-read.md) | **위 넷을 고친 뒤** (기준선이 다르다) | **기능 세로 축** — 추적 파일 555개 81,143줄을 기능 세로줄 25조각으로 나눠 조각마다 통독→보완 통독→반증 셋을 흘려보냈다 (에이전트 102, 확정 403 — 높음 1 / 중간 49 / 낮음 353, 기각 50) | 미처리. 사람이 정할 것은 문서 §8. **기각률 11%라 신뢰도 유보는 그대로다** — 문서 §2에 함께 적혀 있다 |

**`codebase-audit`과 나머지 둘은 성격이 다르다.** `codebase-audit`은 코드를 읽고 결함을
찾은 기록이고, `feature-sweep`·`functional-uiux-sweep`은 **앱을 실제로 눌러 본** 기록이다.
후자는 「기존 검사망을 통과하던 것」을 잡는 데 강하고(단위·통합 테스트가 다 초록인 상태에서
결함 2건이 나왔다), 전자는 아직 증상이 없는 설계 문제를 잡는 데 강하다. 둘 다 필요하다.

2026-08-31 코드 감사가 그것을 다시 보였다 — 같은 날 실조작 점검이 전부 초록이었는데,
「명단에서 빠진 학생에게는 상벌점을 부여할 수 없다」는 방어선이 실제로는 아무것도 막지
않는다는 사실은 코드를 읽어야만 보였다 (그 문서 C-01).

**같은 코드를 다시 읽어도 새것이 나온다.** `-deep`은 앞 감사와 기준선이 같은데 신규 84건을
냈다 — 축을 모듈에서 관심사로 바꾸고(한 모듈만 보면 사소한 일이 전 저장소를 훑으면 규격
균열로 보인다), 설정 파일을 코드와 함께 실제로 실행해 본 결과다. 그 축에서만 보이는 것이
유일한 높음이었다: `docs/deploy.md`대로 설치한 도커 배포에서 명단 반영이 통째로 죽는다
(그 문서 D-01).

**읽는 축이 다르면 잡히는 것이 다르다.** 같은 기준선을 세 번 읽었고 축이 셋이었다 —
모듈 축(`2026-08-31-codebase-audit`) · 관심사 축(`-deep`) · 파일 순서 축(`2026-09-01-full-read`).
셋은 대체 관계가 아니다. 관심사 축은 설정과 런타임 코드를 맞물려 봐야 보이는 배포 실패를
잡았고(`-deep` D-01), 파일 순서 축은 검색어를 세울 수 없는 결함 40건을 잡았다 —
호출부와 컴포넌트를 이어 읽어야 보이는 모달 결함, 통과할 수밖에 없는 단언, 주석이 약속한
것을 코드가 어기는 자리. 반대로 통독은 D-01을 못 찾았다(관련 파일 셋이 서로 다른
에이전트에게 갔다). 한 번만 돌린다면 관심사 축이 효율이 가장 좋았다.

**넷째 축이 그 한계를 메웠다.** `2026-09-01-vertical-full-read`는 통독의 「한 줄도 건너뛰지
않는다」를 유지하되 나누는 단위를 **경로 순서에서 기능 세로줄로** 바꿨다 — 모듈·화면·
컴포넌트·테스트를 한 손에 주고, 조각을 25개로 쪼개 에이전트당 읽는 양을 10,000줄에서
3,250줄로 줄였다. 그 라운드의 유일한 높음이 정확히 파일 순서 축이 놓쳤던 종류다:
배포에 관계된 파일 스물일곱을 한 에이전트가 쥐고 `docs/deploy.md`대로 따라가 보니,
문서가 시키는 대로 만든 비밀번호에 `/`가 들어가면 접속 문자열이 깨져 도커 배포가
통째로 뜨지 않는다(그 문서 `infra-R01`).

**대신 조각당 읽는 양을 줄이면 테스트가 드러난다.** 확정 403건 중 119건이 테스트였고,
통독자가 대상 소스와 테스트를 함께 열고 **변이를 넣어 실제로 스위트를 돌려 볼** 여유가
생긴 것이 원인이다. 출입증 이어 붙이기를 막는 60분 여백을 0으로 바꿔도, 상벌점 집계의
재적·학년도·트랙 조건을 지워도, 학생증 QR의 서명 대상을 바꿔도 2,281건이 전부 통과한다.
**초록불이 무엇을 지키는지는 초록불이 알려 주지 않는다.**

**두 문서를 함께 읽을 때 주의할 것.** `-deep`의 DL-07과 앞 감사의 C-01은 이름이 비슷하지만
**다른 제거 경로**를 말한다 — 「파일에서 줄을 지운 학생」(행째 삭제)과 「파일에 퇴학·전학으로
남은 학생」(살아남아 게이트를 통과)이다. C-01은 여전히 유효하며 높음이다 (`-deep` §4.3).

## 디자인 관련 문서는 `docs/design/`에 있다

| 문서 | 시점 | 무엇을 다루나 |
|---|---|---|
| [`../design/DESIGN-supabase.md`](../design/DESIGN-supabase.md) | — | 원본 시안 (흰 캔버스·에메랄드 하나) |
| [`../design/2026-08-17-redesign-spec.md`](../design/2026-08-17-redesign-spec.md) | 리디자인 착수 | 토큰 치환표·문구 규칙·주석 규칙. **현재 유효한 규격** |
| [`../design/2026-08-17-responsive-audit.md`](../design/2026-08-17-responsive-audit.md) | 리디자인 직후 | 375·768·1024·1440 브라우저 실측. P1~P3 수정 계획 |

## 2026-08-17 리디자인 이후 무엇이 달라졌나

`2026-08-16-codebase-audit.md`를 읽을 때 아래를 함께 알아야 한다. 그 문서가 전제한
디자인·컴포넌트 구조가 바뀌었다.

- **디자인 토큰 전면 교체.** `--color-pri`가 `#00876c` → `#3ecf8e`가 되었고 **배경 전용**이다
  (흰 배경 대비 2:1이라 글자로 쓰면 안 읽힌다). 글자용 초록은 `--color-pri-ink`.
- **`text-display` 토큰은 삭제됐다.** 상단바가 모든 화면에 `<h1>`을 그려 쓸 자리가 없었다.
- **`DataTable`이 생겼다.** 좁은 폭에서 카드 목록으로 바뀌는 표. 기존 `TableFrame`은
  그대로 남아 있고, 표로 세울 수 없는 목록은 둘 다 쓰지 않는다.
- **공용 컴포넌트가 늘었다** — `BackLink`·`SecretPanel`·`StatTile`,
  `SectionCard`의 `variant="panel"`·`tone="danger"`, `Button`의 `size="icon"`.
- **기능 변경 하나** — 규정 삭제가 사유를 받아 감사로그에 남긴다 (`merit:rule:delete`의
  `metadata.reason`).

## 다음에 검토할 만한 것

- **감사로그 IP 보존 기간 정책이 없다.** IP도 개인정보다. 얼마나 오래 남길지, 언제 IP만
  지우고 나머지는 남길지를 업무 모듈이 늘기 전에 정해야 한다.
- **인증코드 발송 남용이 감사로그에 안 보인다.** 대상별 5회/시간·IP별 20회/시간 제한이
  막지만, "누가 얼마나 시도했나"를 되짚을 자료는 없다. (2026-08-26 감사에서 확인:
  제한 자체는 우회 불가능하다. 다만 **지금은 실제 발송을 하지 않는다** — 같은 문서 §3.6.)
- **`/admin/students/import`가 개인정보를 DOM에 담는다.** 오류 리포팅 도구를 붙일 때
  이 화면을 마스킹 대상에 넣어야 한다.
